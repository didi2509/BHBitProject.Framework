using BBP.Test.Selenium.Base;

namespace BBP.Test.Selenium
{
    /// <summary>
    /// Criador padrão de drivers do selenium, para sobrescrever apenas o caminho dos drivers utilize a sobrescrita de métodos (override) na classe (partial)
    /// </summary>
    public sealed partial class DefaultSeleniumDriverCreator : SeleniumDriverCreatorBase
    {
      
    }
}
